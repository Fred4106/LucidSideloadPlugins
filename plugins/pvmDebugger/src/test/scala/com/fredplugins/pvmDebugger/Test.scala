package com.fredplugins.pvmDebugger

import com.fredplugins.pvmDebugger.*

import scala.util.chaining.scalaUtilChainingOps

object Test extends App {
	println("Testing")
	// val source = new SEventSource("Bob")
	// new PvmGui(source).tap(gui => {
	// 	new Thread(() => {
	// 		Thread.sleep(20000)
	// 		gui.closeGui()
	// 	})
	// })

	// source.push(new SNpcSpawned(22, 224, 33, null))
	// println("test")
	// for (i <- 1 to 250) {
	// 	Thread.sleep(100)
	// 	source.push(new SNpcSpawned((i * 7), i, 33 + i * 13 % 7, null))
	// }
}
